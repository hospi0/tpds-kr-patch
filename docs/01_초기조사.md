# Theme Park DS (NDS USA) 한글화 — 초기 조사

## 원본
- `C:\claude\roms\nds\Theme Park DS.nds` — 33,554,432 B, md5 `99d714f2fece28c830dde07c8aeea477`
- 헤더: 제목 `THEMEPARKDS`, 게임 코드 `A8QE`(북미판), NitroFS 파일 693개

## 참고할 기존 작업
- 테마파크 계열 한글화: PS1 테마파크(`tp-ps1-kr-patch`), 신 테마파크(`stp-ps1-kr-patch`), TPW(`tpw-ps1-kr-patch`), TPRC PS2(`tprc-kr-patch`) — 용어표·번역 재사용 후보
- NDS 도구·방법: 시드와 초코보 DS+(`chocods-kr-patch`) — ndspy, NFTR 글꼴, LZ10/LZ11

## 다음
- 파일 목록·텍스트/글꼴 위치 조사

## 제스처(동그라미·V) → 탭 (2026-09-25) — 시험 1
- 스테이트: 메인 RAM = .ml1/.ml2 파일 오프셋 0x24 (= 0x02000000).
- 획 기록 구조체 G = 0x20D937C: +0x48 좌표 버퍼(0x20D9418, s16 x,y, 펜 뗌 = -1,-1), +0x4C 개수, +0x50 용량 0x400, +0x34 획 자국 비트맵, +0x38 제스처 켜짐.
- 판정 함수 0x2084E2C(펜 뗄 때): 테두리 대각선^2(0x2084D0C) ≤ 0xE1 → 탭(버퍼만 비움) / 크면 인식 라이브러리(0x209A6A8·0x209A878) → 'v' = 첫 점으로 0x2041948(x,y,1) / '0'·'o' = 중심(0x2084D94)으로 0x2041948(cx,cy,0)+(cx,cy,1).
- 0x2041948(x,y,mode) = 제스처 대상 목록(10 B: x0,y0,x1,y1 s16, +9 켜짐)에서 좌표가 든 항목 → 0x2041EAC(mode).
- 패치 `tools/tapfix.py`: 0x2085020 `movle fp,#1` → `ble 0x208507C`; 0x2085054~ 세 벌 블록을 한 벌로 줄이고 0x208507C 에 `mov fp,#1 / b 0x2085178`(동그라미 처리로). 시험 롬 `work/tpds_tap1.nds` md5 `66b24293…` → F: `Theme Park DS (tap test1).nds`. ✅실기 «된다»(2026-09-25). ⚠️원본 롬으로 만든 스테이트를 불러오면 RAM 코드가 원본으로 덮인다 — 시험은 새로 부팅.

## 한글화 조사 1차 (2026-09-25)
- 언어는 게임 안 언어 선택 화면(`mainmode_language.c`, `data/langage.NCGR/NSCR/NCLR`)에서 고른다. 파일은 `data/<이름>-<jp|en|fr|gr|es|ita|cod>.dat`. **한국어 = JP 자리 교체** 방침(JP 는 줄바꿈 `\n` 을 손으로 넣고, 글꼴에 CJK 칸이 있음).
- 글꼴 = `DATA/fontAll.nftr` 하나(`/data/fontAll.nftr` 만 로드; `fontUTF16.nftr` 은 안 씀) — 11×13 1bpp, 글리프 3,807(한자 ≈2,900 + 가나·전각). CMAP 마지막 블록이 0‥FFFF 쌍 방식 → 한자 칸을 한글로 갈아 끼움(chocods `nftr.py` 재사용 가능). `number.nftr` = 숫자 전용.
- 텍스트 형식(`tools/langdat.py`, EN·JP 왕복 OK): u16 묶음 수 · u16 묶음별 줄 수 · NUL 끝 UTF-8 문자열 연속(오프셋 표 없음 → 길이 제약은 화면 폭뿐). COD 파일 = 줄마다 식별자(T_BUY_TRACK …).
- 분량(줄): LANG0 997(UI·메시지) · advice0‥3 각 315(조언 — **조언자 4명 말투가 다 다름**, JP 기준 같은 줄 56) · credits 137. 합계 2,394줄, JP ≈75,000자.
- 제어: `<1>`·`<2>`(조언자/색?), `<P>`(쪽 넘김), `%s`, `\n`.
- 남은 조사: 구운 그림 글자(`*_ui.bin` 등 — 언어별 파일이 없어 그림 속 영문/일문이 있을 수 있음), 언어 선택 화면 그림(일본 국기·「日本語」 → 한국), 자판(jp_*.kbdmap — 이름 입력), 줄 폭(JP 줄 칸수 분포).

## PoC 1 (2026-09-25) — 스크린샷 6장 화면 문구 전부
- 번역 `work/ko/00_poc.tsv`(파일·묶음·번호·KO) 142줄 → `tools/build.py work/ko <출력>`: JP DAT 줄 교체 + 글꼴 한자 칸에 갈무리9 한글 290자(남은 일본어가 쓰는 한자 833 은 보존, 글꼴 크기 불변) + 탭 패치.
- 한글 글리프 = 11×13 칸 2‥10행(일본어와 같은 자리), 폭 (1, w, 10).
- 시험: 19/2(한마디) 는 `\n` 없이 한 줄 — 자동 줄바꿈 확인용. 공백 전진폭이 1px(원본 글꼴) — 한국어 띄어쓰기가 좁게 보이면 글꼴 공백 폭을 넓힌다.
- `work/tpds_poc1.nds` md5 `c9e15f50…` → F: `Theme Park DS (KR poc1).nds`. ⏳실기.

## 본번역 빌드 test1 (2026-09-25)
- 클라우드 번역(브랜치 claude/gifted-galileo-tz9h3z) 합침 → 2,178줄 검사 오류 0, 한글 810자, 남은 한자 0.
- `work/tpds_kr_t1.nds` md5 `95e71003…` → F: `Theme Park DS (KR test1).nds`. ⏳실기(대사창 밖 넓은 줄 5: 4/3 결산표 머리줄, 6/9‥13 연구 설명 — `tools/widecount.py`).
- t3 (2026-09-25): 기본 언어 = 한국어 — `tools/tapfix.py` 에서 OS_GetOwnerInfo(0x2099854) 의 언어 읽기 0x209986C `lsl r2,r2,#0x1d` → `mov r2,#0`(본체 언어 무시, 0번 = JP 자리 = 한국어). md5 는 빌드 출력 참조. ⏳실기.
- t4 (2026-09-25): 이름 입력 가나 자판 8쪽 → 한글 368음절(`tools/kbdko.py`, 빌더 연결) — かな=오십음 자리(아이으에오·가기그게고…) · カナ=ㅓㅜㅐㅕㅠ · ゛=거센·된소리 · ゜/小字=받침 음절 184(이름 흔한 음절 우선, 가나다순). 부호 ！？、。ー 제자리. 글꼴 한글 842자. md5 `18be751d…` → F: `Theme Park DS (K).nds`. ⏳실기. 자판 왼쪽 かな/カナ/゛/゜/小字 버튼 글자는 그림(손 안 댐).
