# 테마파크 DS 한글화 — 이어하기(클라우드·새 세션용)

비공개 저장소. ROM·세이브스테이트·빌드 결과물은 없다(로컬에만). **클라우드에서는 번역만** 하고, 빌드·실기는 로컬에서 한다.

## 1. 현재 상태 (2026-09-25)
- ✅ **동그라미·V 제스처 → 탭** 패치 실기 성공(`tools/tapfix.py`, 사용자 «완벽히 작동»). 이 게임 한글화의 전제 조건이었다.
- ✅ 텍스트 구조·글꼴 조사 끝(`docs/01_초기조사.md`). 그래픽 글자는 한글화할 것 없음(사용자 판단).
- ⏳ PoC 1(스크린샷 6장 화면 문구 142줄, `work/ko/00_poc.tsv`) 실기 확인 중 — 한글 모양 · 공백 폭(원본 1px) · 자동 줄바꿈(19/2 일부러 한 줄).
- ✅ **본번역 완료**(2026-09-25, 클라우드): `work/ko/LANG0.tsv` 783 · `advice0‥3.tsv` 각 307 · `credits.tsv` 25(직함만, 이름은 원문 유지) — PoC 포함 2,178줄. 가나·한자가 남은 미번역 줄 0, 검사기 오류는 PoC 19/2(자동 줄바꿈 시험용, 일부러) 하나뿐. 쓴 한글 음절 810자(모두 완성형 2,350 안).
- 다음 = **로컬 빌드·실기**(`python tools/build.py work/ko <출력.nds>`) → 메뉴·표 칸 넘침, 조언자 말투 확인.

## 2. 번역할 텍스트 (`work/text/*.tsv` — 전량 추출본)
| 파일 | 줄 | 내용 |
|---|---|---|
| LANG0.tsv | 997 | 메뉴·UI·메시지·조언자 소개 |
| advice0‥3.tsv | 각 315 | 조언자 대사 — **4명 말투가 다름**(JP 기준 같은 줄 56뿐) |
| credits.tsv | 137 | 제작진(대부분 영문 이름 — 직함만 번역) |

열: 묶음 · 번호 · 식별자(COD) · EN · JP · **KO(비어 있음)**. `\n` = 줄바꿈, `<P>` = 쪽 넘김.
- 번역은 **JP 기준**(한국어 = JP 자리 교체), EN 은 뜻 확인용.
- 작업 방식: `work/text/X.tsv` 를 `work/ko/X.tsv` 로 복사해 KO 열을 채운다(검사기·빌더가 열 수로 알아본다). PoC 파일 `00_poc.tsv`(파일·묶음·번호·KO 4열)와 겹치는 줄은 한쪽만 남길 것(빌더가 중복이면 멈춘다).
- 조언자(LANG0 23 의 순서): 리(22세, 신진기예) · Mr.벨(58세, 박학다재) · 크리스탈(26세, 재색겸비) · 모카(18세, 순진무구).
- advice 파일 ↔ 인물(JP 말투로 판단, 실기 확인 전): **0 = Mr.벨(로버트)** 존댓말 «~습니다/~해 주십시오» · **1 = 리** «ボク»·«スゲェ» → «~해요!» · **2 = 크리스탈** «頂けますか»·«わ/かしら» → 지시는 «~해 주시겠어요?», 잡담은 여성 반말 · **3 = 모카** «だよ/だね»·자기 이름을 1인칭 → 반말(튜토리얼 앞부분만 긴장한 존댓말). 번역은 이 대응으로 네 벌.

## 3. 조판 규칙 (검사기 `python tools/kocheck.py` 가 막는다 — 배치마다 돌릴 것)
- 대사창 한 줄 = **220px(전각 22자)**, 한 쪽 **3줄**. 한글 10px · 반각(영숫자·ASCII 부호) 5px · 공백 4px 로 계산.
- 줄은 `\n` 으로 직접 끊는다(JP 처럼). 낱말 한복판에서 끊지 말 것.
- `%s` `%d` `<1>` `<2>` `<P>` `＠＠＠＠` 는 원문과 개수가 같아야 한다.
- 문장부호(, . ! ? : ;) 뒤 공백은 **빌더가 지운다**(전 프로젝트 규칙) — 폭 계산도 지운 뒤 기준.
- 번역에 가나·한자를 남기지 말 것(글꼴 한자 칸을 한글로 쓴다).
- 일본어 고유명사는 거센소리(か=카, た=타, つ=츠).
- 메뉴·표 항목(짧은 줄)은 원문 폭(JP 글자 수)을 넘지 않게 — 칸이 좁다(검사기는 아직 대사창 폭만 본다).
- 원문 한 줄이 이미 220px 를 넘는 줄(표 머리줄 LANG0 4/3, 연구 설명 6/9‥14 등)은 원문 폭까지 허용(검사기).
- ○로 둘러싸기·V 체크 안내는 탭 패치에 맞춰 «터치»로 바꿨다(PoC 와 같은 방침).
- 본번역에서 정한 것: 整列歩道=대기줄, スイーパー=청소부, メカニック=정비공, ガードマン=경비원, エンターテナー=엔터테이너, ジェットコースター=롤러코스터, セーブ/ロード=세이브/로드, ギブアップ=포기, 〜くん=〜 군, カーソルフリー=커서 프리, ピンセット機能=핀셋 기능, 導線=동선. %s 뒤 조사는 «을(를)»·«(으)로» 식이거나 조사 없이.
- 용어는 PS1 테마파크 계열 한글판과 맞춘다(로컬 `tp-ps1-kr-patch`·`tpw-ps1-kr-patch` 용어표 — 이 저장소엔 없음). PoC 에서 정한 것: アトラクション=어트랙션, ショップ=상점, スタッフ=직원, 興奮度=흥분도, 耐久性=내구성, 定員数=정원, すれちがい通信=엇갈림 통신, 総資金=총자금.
- 번역 줄은 셸 이스케이프 말고 편집기로 직접 쓸 것(음절이 깨진다).

## 4. 도구
- `tools/langdat.py` — 추출(`python tools/langdat.py` → work/text/*.tsv, ROM 필요) · parse/build.
- `tools/kocheck.py` — 번역 검사(ROM 불필요).
- `tools/build.py work/ko <출력.nds>` — 빌드(로컬, ROM·갈무리9 필요). 글꼴 한자 칸에 «쓴 한글 음절»만 넣고 남은 일본어의 한자는 보존. 완성형 2,350자 전부 들어갈 칸(한자 2,966)이 있다.
- `tools/tapfix.py` — 제스처→탭(빌더가 자동 적용).
- `tools/nftr.py`·`tools/bdf.py` — 글꼴 읽기·쓰기(초코보 DS+ 프로젝트에서 가져옴).
- `tools/findjp.py 문구…` — 화면 문구가 어느 줄인지.
